#include <bits/stdc++.h>
using namespace std;

int main()
{
    int x;
    cout << "Enter The age: ";
    cin >> x;
    if(x > 18)
    {
        cout << "The age is greater than 18";
    }
    else{
        cout << "The age is not greater than 18";
    }
    return 0;
}